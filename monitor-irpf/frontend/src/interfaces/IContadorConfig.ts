/** Cadastro do contador em app_kv (integrações / procuração). */
export interface ContadorConfig {
  nome: string;
  crc: string;
  cpf: string;
  cnpj: string;
  email: string;
  telefone: string;
}
