# =============================================================================
# Carteira IRPF — frontend (Vite)
# O cliente chama sempre /api no mesmo host; o Vite faz proxy para o backend.
# Normalmente não precisas de variáveis aqui.
# =============================================================================

# Se no futuro quiseres API absoluta (sem proxy), podes usar:
# VITE_API_BASE_URL=http://127.0.0.1:8000
