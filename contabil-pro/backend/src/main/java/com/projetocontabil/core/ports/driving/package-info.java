/**
 * Portas DRIVING (entrada) — interfaces expostas pela Application Layer
 * para serem consumidas pela camada de apresentação (Controllers).
 *
 * Ex: OnboardingInputPort, RotinasInputPort, AlvarasInputPort.
 */
package com.projetocontabil.core.ports.driving;
